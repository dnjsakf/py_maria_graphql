/* React */
import React from 'react';

/* Main Component */
const Setting = props =>{
  const {
    className,
    ...rest
  } = props;

  /* Rendering */
  return (
    <h1>Setting</h1>
  );
}

export default Setting;